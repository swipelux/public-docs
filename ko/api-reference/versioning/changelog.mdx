---
title: "변경 이력"
description: "Swipelux API의 추가, 변경 및 지원 중단 사항을 추적합니다."
---

이 페이지에는 Swipelux API에 대한 날짜별 변경 사항이 나열되어 있습니다. 각 항목에는 영향을 받는 버전, 변경 유형, 그리고 통합 담당자에게 요구되는 조치가 명시되어 있습니다.

기존 통합을 이전 버전에서 업그레이드하려면 [v3로 마이그레이션](/ko/api-reference/versioning/migrate-to-v3)을 참조하십시오.

## 미출시

_출시 예정인 변경 사항은 배포 전에 여기에 나열됩니다._

## 2026-08-24

**버전:** v3

### 추가됨
- API 키가 이제 Rules API 권한을 노출하므로 기존 범위와 함께 규칙 관리로 키의 범위를 지정할 수 있습니다. [인증](/ko/integration/authentication)을 참조하세요.
- 최초 payin 온보딩은 이제 진행하기 전에 Polygon 고객 지갑이 필요합니다. [계정](/ko/integration/accounts)을 참조하세요.

### 변경됨
- KYB 첫 페이지에서 더 이상 대표자의 생년월일을 수집하지 않습니다. 워크플로의 이후 단계에서 수집됩니다. [KYB 워크플로](/ko/knowledge-base/business-onboarding/kyb-workflow)를 참조하세요.
- 문서 업로드는 이제 표준 `image/jpeg` 외에 `image/jpg` MIME 별칭도 허용합니다. [문서 요구 사항](/ko/knowledge-base/business-onboarding/document-requirements)을 참조하세요.
- 작업 이력에서 내부 리비전 카운터가 숨겨졌습니다. 검토자는 더 깔끔하고 사람이 읽기 쉬운 감사 추적을 볼 수 있습니다. [기능 및 요구 사항](/ko/integration/onboarding/capabilities-and-requirements)을 참조하세요.
- UAEFTS v1 계정 설정이 중단되었습니다. 새로운 AED 지급 계정은 현재 레일을 사용합니다.

### 수정됨
- RFI 양식은 검토자가 새 문서 드롭을 추가할 때 기존 입력 항목을 보존하므로 재제출 시 이전에 제공된 답변이 더 이상 손실되지 않습니다.
- 컴플라이언스 세션은 유효한 링크를 보존하고 세션 계약에서 반환된 명시적 액션을 따르므로 중단된 연속 처리가 제거됩니다.
- 컴플라이언스가 ToS 여정에 대해 표준 범위 이름을 허용하므로, 문서화된 범위 값을 사용하는 통합이 올바르게 완료됩니다.
- 결제 액세스는 이제 오래된 고객 소유의 인테이크 대신 애플리케이션 소유의 인테이크를 엽니다.
- KYB 데모 온보딩이 지갑 계약과 정렬되며, 가맹점 자체 고객 항목이 더 이상 고객 목록에 표시되지 않습니다.
- API는 이제 요청을 자동으로 정규화하지 않고 잘못된 형식의 쿼리 문자열 인코딩을 `400` 오류로 거부하므로 잘못된 입력이 더 일찍 드러납니다. [오류 및 재시도](/integration/errors)를 참조하세요.
- 문서 업로드 엔드포인트는 본문이 비어 있거나 형식이 잘못된 요청을 `400` 오류로 거부합니다.

## 2026-08-17

**버전:** v3

### 추가됨
- OpenAPI 스펙에 v3 Problem Details 카탈로그를 게시하여, 통합 담당자가 스키마에서 직접 오류 응답을 매핑할 수 있도록 했습니다. [Errors](/integration/errors).
- v3 계좌 웹훅 이벤트에 `customerId`를 추가하여, 후속 읽기 없이 계좌 활동을 소유 고객과 연관시킬 수 있습니다. [웹훅](/ko/integration/webhooks)을 참조하십시오.
- 머천트 패널에서 V3 개인 프로필 필드를 완전히 지원하여, 온보딩 담당자가 v3 API를 통해 수집된 모든 KYC 필드를 검토하고 편집할 수 있습니다. [고객 온보딩](/ko/integration/onboarding/customers)을 참조하십시오.
- v3 개발자 마이그레이션 가이드를 게시했습니다. [v3로 마이그레이션](/ko/api-reference/versioning/migrate-to-v3)을 참조하십시오.

### 변경됨
- 고객 및 계좌의 국가 코드는 쓰기 시점에 할당된 ISO 3166-1 alpha-2 값으로 검증됩니다. 읽기는 레거시 저장 값을 허용하므로, 기존 레코드는 계속 해석됩니다. [계좌와 지갑](/ko/integration/accounts)을 참조하십시오.
- USDC 가용성은 이제 목적지 네트워크에 따라 범위가 지정되므로, 견적과 전송은 목적지가 실제로 지원하는 네트워크에서만 USDC를 제공합니다.
- `Capability.ready`는 권한 승인을 나타내며 계좌 발급을 의미하지 않습니다. 계좌 발급 상태는 `accountProvisioning`에서 확인하세요: `not_applicable`, `not_started`, `in_progress`, `issued`, `failed`.
- iPhone에서 업로드된 HEIC 파일이 신원 및 주소 증명 서류로 이제 허용됩니다.

### 수정됨
- 이미 소비된 견적을 재실행하면 일반 오류가 아닌 `409 quote_execution_consumed`을 반환합니다. [견적과 전송](/ko/integration/quotes-and-transfers)을 참조하십시오.
- 전송 목록 읽기와 견적 금액 읽기가 대규모 과거 결과에서 더 이상 `500` 오류를 발생시키지 않습니다.
- 개인 고객에 대한 관계자 읽기가 더 이상 `404`를 반환하지 않습니다.
- 머천트 패널의 검증 링크 행에서 모든 행에 **Open** 액션이 표시되고, 모든 지갑 인프라 태스크 링크가 요구 사항 뷰에 렌더링됩니다.

{/* Entry template

  ## YYYY-MM-DD

  **Version:** v3

  ### Added
  - Short description of the new endpoint, field, or event.

  ### Changed
  - Short description of the behavior or schema change. Note whether it is backward compatible.

  ### Deprecated
  - Endpoint or field being phased out, along with the replacement and removal date.

  ### Removed
  - Endpoint or field that is no longer available.

  ### Fixed
  - Bug fix visible to integrators.

  */}
